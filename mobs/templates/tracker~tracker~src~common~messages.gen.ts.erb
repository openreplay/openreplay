// Auto-generated, do not edit
/* eslint-disable */

export declare const enum Type {
  <%= $messages.select { |msg| msg.tracker }.map { |msg| "#{ msg.name } = #{ msg.id }," }.join "\n  " %>
}

<% $messages.select { |msg| msg.tracker }.each do |msg| %>
export type <%= msg.name %> = [
  /*type:*/ Type.<%= msg.name %>,
  <%= msg.attributes.map { |attr| "/*#{attr.name.camel_case}:*/ #{attr.type_js}," }.join "\n  " %>
]
<% end %>

export const ASSET_MESSAGES: ReadonlySet<number> = new Set([
  <%= $messages.select { |msg| msg.pipeline == 'a' }.map { |msg| "#{msg.id}," }.join "\n  " %>
])

export const DEVTOOLS_MESSAGES: ReadonlySet<number> = new Set([
  <%= $messages.select { |msg| msg.pipeline == 'd' }.map { |msg| "#{msg.id}," }.join "\n  " %>
])

export const ANALYTICS_MESSAGES: ReadonlySet<number> = new Set([
  <%= $messages.select { |msg| msg.pipeline == 'b' }.map { |msg| "#{msg.id}," }.join "\n  " %>
])

type Message =  <%= $messages.select { |msg| msg.tracker }.map { |msg| "#{msg.name}" }.join " | " %>
export default Message
