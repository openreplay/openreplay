import { Button } from "@/shadcn-components/button";

const Home = () => {
  return (
    <div className="text-center text-3xl font-bold">
      <Button>home</Button>
    </div>
  );
};

export default Home;
