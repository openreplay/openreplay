apiVersion: v1
data:
  {{- (.Files.Glob "dashboards/*").AsConfig | nindent 2 }}
kind: ConfigMap
metadata:
  labels:
    app: kube-prometheus-stack-grafana
    grafana_dashboard: "1"
    release: monitoring
  name: nginx-igress
  namespace: observability
