# Copy to .env and fill in. Consumed at build time via
#   flutter run --dart-define-from-file=.env
# .env is gitignored; this file is the committed template.
OR_PROJECT_KEY=
OR_SERVER_URL=https://api.openreplay.com/ingest
